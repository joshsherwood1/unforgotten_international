<h2 style="text-align: center">Search Results</h2>
<% @people.each do |person| %>
<div class="row-fluid">
  <div class="text-center col-sm-4"">
    <div class="mx-5">
    <h3><%=link_to person.name, "/people/#{person.id}" %></h3>
    <a href = "/people/<%=person.id%>">
    <img src="<%=person.picture%>"></img></a>
    </div>
  </div>
</div>

<% end %>
